const UserModel = require('./user.model');
const OptionModel = require('./option.model');
const ActiveRecordModel = require('./activerecord.model');

module.exports = {
  UserModel,
  OptionModel,
  ActiveRecordModel,
};
